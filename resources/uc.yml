resources:
  registered_models:
    inventory_optimization:
      name: ${var.model_name}
      catalog_name: ${var.catalog}
      schema_name: ${var.schema}
      comment: "Champion inventory replenishment optimizer logged from the notebook with MLflow Models From Code."

    inventory_optimization_cuopt:
      name: ${var.gpu_model_name}
      catalog_name: ${var.catalog}
      schema_name: ${var.schema}
      comment: "NVIDIA cuOpt inventory replenishment optimizer logged from the serverless GPU notebook with MLflow Models From Code."
